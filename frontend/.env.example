# Auto-generated by `npx convex dev` into .env.local — do not hand-edit those.
# Documented here so a fresh clone knows what's needed.
CONVEX_DEPLOYMENT=
VITE_CONVEX_URL=http://127.0.0.1:3210
VITE_CONVEX_SITE_URL=http://127.0.0.1:3211

# Set via `npx convex env set` (stored in Convex, not this file):
#   SITE_URL, JWT_PRIVATE_KEY, JWKS

# Dodo Payments (client-side publishable key, if needed for checkout widget)
VITE_DODO_PUBLIC_KEY=
